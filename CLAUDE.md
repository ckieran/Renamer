@AGENTS.md

## Claude Code

Branch naming follows the conventional commits convention defined in
`docs/specs/070-engineering-contract.md`: `<type>/<slice-id>-<description>`.
Use `claude/` only for exploration or spec-only work — implementation
branches must use the correct commit type prefix before the first code commit.
